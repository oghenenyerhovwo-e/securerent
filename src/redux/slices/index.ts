import {
    setUser,
    clearUser,
} from "./userSlice"

import {
    toggleTheme, 
    setTheme,
} from "./themeSlice"

import userReducer from "./userSlice"
import themeReducer from "./themeSlice"

export {
    setUser,
    clearUser,
    userReducer,
    toggleTheme, 
    setTheme,
    themeReducer,
}